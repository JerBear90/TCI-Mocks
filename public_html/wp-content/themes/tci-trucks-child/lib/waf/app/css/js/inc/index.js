const Form = require('./Form');
const Field = require('./Field');
const Fieldset = require('./Fieldset');
const Input = require('./Input');
const Duplicator = require('./Duplicator');
module.exports = { Form, Field, Fieldset, Input, Duplicator }